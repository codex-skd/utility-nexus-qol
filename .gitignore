# Gradle
build/
.gradle/
gradle/wrapper/gradle-wrapper.jar

# Minecraft dev environment
run/

# IDE
.vscode/
.idea/
.eclipse/
*.iml
*.ipr
*.iws
*.class
.project
.classpath
.settings/
bin/

# OS
Thumbs.db
.DS_Store
Desktop.ini

# Temp files (investigations, prototypes, extracted JARs)
temp/

# Session libraries (not real dependencies)
lib_ext/

# Optional dev-only compat jar: Waystones is All Rights Reserved, not ours to
# redistribute through the public mirror. Devs drop it into libs/ by hand for
# `runClient`; the feature itself never links against the Waystones API.
libs/waystones-*.jar
libs/balm-*.jar

# Env
.env
*.local

# Graphify — solo el snapshot actual se versiona, nunca copias fechadas
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
